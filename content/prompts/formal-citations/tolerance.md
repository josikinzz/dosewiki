Evaluate tolerance claims conservatively.

- Tolerance timing and cross-tolerance statements should only be `supported` when the packet directly states them.
- If the packet discusses tolerance in broader terms than the article claim, use `needs_review`.
- If no direct support appears, use `needs_source`.
