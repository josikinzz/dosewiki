# data/

Tracked datasets a third party can download and use without running the site. Everything here is loaded by source under `src/`, `lib/`, `server/`, or `scripts/` through the `@data/*` alias (TypeScript) or a relative path (`.mjs`). Generated build artifacts (`*.generated.json`, theme baselines, icon data, social-card manifests, route availability) stay beside their consumers; runtime configuration stays under `src/data/config/`; prompts and editorial copy live under `content/`.

`data/lineage.json` is the provenance contract for every operational artifact in the repository: role, producer, consumers, inputs, edit policy, and the refresh command. `npm run provenance:check` validates it. Producer scripts write per-run sidecars into the gitignored `data/.lineage/` directory.

Licenses: datasets authored by dose.wiki are released under CC0 1.0 as described at `/docs/license` (section 3). Third-party datasets under `data/third-party/` carry their own terms, restated in the `LICENSE.md` beside each one and inventoried in the repository `NOTICE.md`. Records that describe people or creator-retained media are provenance records, not licensed content: the rights in the underlying artwork stay with the artist.

| File | Description | License | Producer |
| --- | --- | --- | --- |
| `lineage.json` | Provenance contract for every operational data artifact (paths, producers, consumers, edit policies, refresh commands) | MIT (repository source) | Data workflow maintainers; validate with `npm run provenance:check` |
| `substances/chemicalIndexManual.json` | Chemical-class taxonomy: class keys, labels, aliases, parents, descriptions, icon keys, and canonical Markush SMILES per class | CC0 1.0 | Chemical taxonomy maintainers; seed class drawings with `npm run molecules:seed-classes` |
| `substances/psychoactiveIndexManual.json` | Psychoactive-class taxonomy: categories, definitions, warnings, icon keys, and aliases that drive the substance index tabs and category routes | CC0 1.0 | Psychoactive taxonomy maintainers; edit by hand, then `npm run generate:icon-data` |
| `substances/routeSynonyms.json` | Route-of-administration labels and their synonyms | CC0 1.0 | Route taxonomy maintainers; edit by hand |
| `substances/usStateFlagMappings.json` | US state and territory names mapped to the `public/flags` icon shown on legality rows | CC0 1.0 | Legality section maintainers; edit by hand |
| `schemas/chemical-index-manual.schema.json` | JSON Schema for the chemical index manual layout document (`categories`, `notes`, `version`) edited through Dev Tools and seeded by `scripts/seed/seed-index-layouts.mjs` | MIT (repository source) | Hand-authored |
| `schemas/mechanism-index-manual.schema.json` | JSON Schema for the mechanism index manual layout document (`categories`, `notes`, `version`) | MIT (repository source) | Hand-authored |
| `schemas/psychoactive-index-manual.schema.json` | JSON Schema for the psychoactive index manual layout document (`categories`, `notes`, `version`) | MIT (repository source) | Hand-authored |
| `legality/core-countries.md` | Country coverage list for legality research runs: every run accounts for each listed country with a sourced entry or an explicit gap | CC0 1.0 | Legality section maintainers; edit by hand, read by `npm run legality:export` |
| `chemistry/generatedSmiles.json` | RDKit-validated SMILES overrides keyed by substance slug, with `represents` for plants, preparations, and brands | CC0 1.0 (dose.wiki compilation; SMILES strings are chemical facts resolved from PubChem and Wikipedia) | `npm run molecules:merge-smiles` (`scripts/data/mergeGeneratedSmiles.mjs`) |
| `chemistry/iupacSmilesMap.json` | IUPAC name to SMILES map resolved through the OPSIN web service; `null` marks names OPSIN could not parse | CC0 1.0 (OPSIN resolutions of public IUPAC names) | `npm run generate:smiles` (`scripts/data/generateSmilesMap.mjs`) |
| `chemistry/titleIupacSmiles.json` | Legacy title, IUPAC name, and SMILES triples from the pre-OPSIN resolution pass | CC0 1.0 | Frozen legacy map; read by `scripts/data/resolveSmilesCoverage.mjs` |
| `chemistry/smiles-coverage.json` | Per-substance SMILES resolution (inline, title map, IUPAC map, or gap-fill) that the molecule seeder reads for articles without inline SMILES | CC0 1.0 (dose.wiki compilation) | `npm run molecules:smiles-coverage` (`scripts/data/resolveSmilesCoverage.mjs`) |
| `chemistry/smiles-gaps.json` | Substances still lacking a SMILES after resolution; the input list for the next gap-fill batch | CC0 1.0 | `npm run molecules:smiles-coverage` |
| `chemistry/smiles-gap-fills/batch-*-input.json`, `batch-*-output.json` | Reviewed SMILES gap-fill research batches: the substances that lacked inline SMILES and the PubChem or Wikipedia structures found for them | CC0 1.0 (dose.wiki compilation) | Gap-fill research batches; merged by `npm run molecules:merge-smiles` |
| `effects/subjectiveEffectsProvenance.json` | Archive.org capture dates and counts for the PsychonautWiki subjective-effect prose dose.wiki restores; served at `/subjective-effects-provenance.json` | CC0 1.0 (manifest); the captured prose is CC BY-SA 4.0 per `/docs/license` section 5 | `npm run generate:subjective-effects-provenance` |
| `effects/effectIndexFeaturedReplications.json` | Ordered editorial list of featured replication slugs recovered from the legacy effectindex.com dump; homepage fallback when the `siteConfig` document has never been curated | CC0 1.0 (list only; artwork rights stay with each artist) | Effect Index editors; seed with `scripts/replications/seed-featured-replications.mjs` |
| `effects/replicationSlugAliases.json` | Replication slug renames (old to new) that the middleware redirects | CC0 1.0 | `scripts/replications/normalize-slugs.mjs` |
| `i18n/glossary/<locale>.json` | Review snapshot of the Postgres translation glossary per locale (term, target, kind, status); never read at runtime | CC0 1.0 | `npm run translate:glossary -- --locale <code> --export` |
| `i18n/glossary/zh-Hans.drafts-2026-09-12.json` | Hand-drafted zh-Hans renderings for the 298 terms added on 2026-09-12, loaded as draft rows for native review | CC0 1.0 | Glossary drafters; imported with `npm run translate:glossary:import-drafts` |
| `i18n/glossary/glosses/{classes,effects,site}.json` | One-line English glosses per glossary term, split by topic, seeded into `translationGlossaryTerms` | CC0 1.0 | Glossary writers; load with `npm run translate:glosses` |
| `contributors/userProfiles.json` | Legacy contributor profile seed (key, display name, bio, links) used to backfill Postgres `contributorProfiles` | Not CC0: describes people; provenance and migration seed only | Legacy contributor profile export; preview with `node scripts/data-ops/migrate-user-profiles-to-data.mjs --dry-run` |
| `contributors/contributorAvatarSources.json` | Provenance for every avatar cropped from a contributor's own replication artwork | Not CC0: provenance record; artwork rights stay with the artist | `bun scripts/contributors/generate-avatars-from-replications.ts --write` (confirmed runs only) |
| `changelog/devChangeLog.json`, `changelog/devChangeLog.archive.json` | Editor save history committed through the Dev Tools change log (recent entries and the rolled-over archive) | CC0 1.0 (edit history of CC0 article content) | Dev Tools change log commit workflow |
| `third-party/tripsit/tripsit-combos.json` | TripSit drug-combination chart keyed by dose.wiki slug | TripSit's terms: non-commercial, attribution, link back, quick-reference note (`third-party/tripsit/LICENSE.md`) | TripSit snapshot; re-keyed by `scripts/parsers/tripsit-combos.ts` |
| `third-party/tripsit/combo_definitions.json` | TripSit combination status legend (Dangerous, Unsafe, Caution, the three Low Risk variants, Unknown) | TripSit's terms (`third-party/tripsit/LICENSE.md`) | TripSit snapshot |
| `../public/SubstanceIndex.json` | Archival sanitized snapshot of the substance article corpus, the download linked from the About page; stays under `public/` because it is served at `/SubstanceIndex.json` | CC0 1.0 (article text) with the exceptions in `/docs/license` section 4 | `node scripts/data/sanitize-public-substance-archive.mjs` |

The live open-data downloads (`/open-data/SubstanceIndex.json`, `/open-data/EffectIndex.json`, `/open-data/TripReports.json`) are rendered from Postgres by `src/app/open-data/` and are not tracked files.

The ProtestKit reagent-test snapshot (`third-party/protestkit/reagentTests.json`, read by `npm run reagents:import`) is fetched locally with `node scripts/data/cacheProtestKitReagentTests.mjs`, requires ProtestKit permission, and is never committed: the directory is gitignored.
