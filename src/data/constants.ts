// Canonical definition lives in the schema layer; re-exported here so the
// data/builder layer keeps its existing import sites.
export { UNQUALIFIED_MECHANISM_QUALIFIER_KEY } from "../schema/substance/mechanismNormalization";
